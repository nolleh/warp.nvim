-- Entry point for warp.nvim
-- This file is automatically loaded by Neovim

if vim.g.loaded_warp then
  return
end
vim.g.loaded_warp = true

-- Setup command will be created by the main module
-- User should call require("warp").setup() in their config
